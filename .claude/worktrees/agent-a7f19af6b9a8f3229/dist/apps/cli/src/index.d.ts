#!/usr/bin/env node
export declare class SecManCLI {
    private core;
    private program;
    constructor(projectRoot: string);
    private setupCommands;
    run(args: string[]): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map